# Swaraj_07_LAB3_DL - Line by Line Code Explanation

**Notebook:** `Swaraj_07_LAB3_DL.ipynb`

**Aim:** K-Nearest Neighbors classification on the Iris dataset with scaling, manual prediction collection, and classification metrics.

This document is written for examiner/viva preparation. Each code line is listed with its role in the program.

## Code Cell 2

| Line | Code | Explanation |
|---:|---|---|
| 1 | `from sklearn.datasets import load_iris` | Imports load_iris for loading the built-in Iris dataset. |
| 2 | `from sklearn.model_selection import train_test_split` | Imports train_test_split for dividing data into training and testing sets. |
| 3 | `from sklearn.neighbors import KNeighborsClassifier` | Imports KNeighborsClassifier for KNN classification. |
| 4 | `from sklearn.preprocessing import StandardScaler` | Imports StandardScaler for converting features to mean 0 and standard deviation 1. |
| 5 | `from sklearn.metrics import accuracy_score, confusion_matrix, classification_report` | Imports accuracy_score for classification accuracy, confusion_matrix for summarizing correct and wrong class predictions, classification_report for precision, recall, F1-score, and support. |
| 6 | `` | Blank line used to separate code logically and improve readability. |
| 7 | `` | Blank line used to separate code logically and improve readability. |

## Code Cell 3

| Line | Code | Explanation |
|---:|---|---|
| 1 | `iris = load_iris()` | Loads the Iris dataset object, including features, labels, and class names. |
| 2 | `X = iris.data` | Stores the Iris numeric feature matrix. |
| 3 | `y = iris.target` | Stores the Iris class labels. |

## Code Cell 4

| Line | Code | Explanation |
|---:|---|---|
| 1 | `scaler = StandardScaler()` | Creates a StandardScaler object for feature standardization. |
| 2 | `X_scaled = scaler.fit_transform(X)` | Fits the scaler on the data and transforms the values into scaled form. |

## Code Cell 5

| Line | Code | Explanation |
|---:|---|---|
| 1 | `X_train, X_test, y_train, y_test = train_test_split(` | Splits features and target into training and testing datasets. |
| 2 | `    X_scaled, y, test_size=0.3, random_state=42` | Passes the data and split settings into train_test_split. |
| 3 | `)` | Closes the multi-line statement or data structure started above. |

## Code Cell 6

| Line | Code | Explanation |
|---:|---|---|
| 1 | `k = 5` | Stores an important hyperparameter used later in the notebook. |
| 2 | `knn = KNeighborsClassifier(n_neighbors=k)` | Creates a KNN classifier with the chosen number of neighbors. |
| 3 | `knn.fit(X_train, y_train)` | Trains/fits the model or estimator using the provided data. |
| 4 | `` | Blank line used to separate code logically and improve readability. |

## Code Cell 7

| Line | Code | Explanation |
|---:|---|---|
| 1 | `y_pred = []` | Stores predicted output values generated by the model. |
| 2 | `` | Blank line used to separate code logically and improve readability. |
| 3 | `for i in range(len(X_test)):` | Starts a loop that repeats the indented block for each item in the range or sequence. |
| 4 | `    pred = knn.predict([X_test[i]])` | Generates predictions using the trained model. |
| 5 | `    y_pred.append(pred[0])` | Adds the selected value or sequence to the list. |

## Code Cell 8

| Line | Code | Explanation |
|---:|---|---|
| 1 | `print("Index\tActual\t\tPredicted")` | Prints the given message or variable value for inspection. |
| 2 | `print("--------------------------------------")` | Prints the given message or variable value for inspection. |
| 3 | `` | Blank line used to separate code logically and improve readability. |
| 4 | `correct = 0` | Initializes the counter used for manual accuracy calculation. |
| 5 | `` | Blank line used to separate code logically and improve readability. |
| 6 | `for i in range(len(y_test)):` | Starts a loop that repeats the indented block for each item in the range or sequence. |
| 7 | `    actual_label = iris.target_names[y_test[i]]` | Stores the Iris class labels. |
| 8 | `    predicted_label = iris.target_names[y_pred[i]]` | Stores the Iris class labels. |
| 9 | `` | Blank line used to separate code logically and improve readability. |
| 10 | `    print(f"{i}\t{actual_label}\t{predicted_label}")` | Prints the given message or variable value for inspection. |
| 11 | `` | Blank line used to separate code logically and improve readability. |
| 12 | `    if y_test[i] == y_pred[i]:` | Checks whether the prediction matches the actual label before counting it as correct. |
| 13 | `        correct += 1` | Increments the counter by 1 when the condition is true. |

## Code Cell 9

| Line | Code | Explanation |
|---:|---|---|
| 1 | `manual_accuracy = correct / len(y_test)` | Stores the computed value in \`manual_accuracy\` for use in later steps. |
| 2 | `print("\nManual Accuracy:", manual_accuracy)` | Prints the calculated accuracy value. |
| 3 | `` | Blank line used to separate code logically and improve readability. |

## Code Cell 10

| Line | Code | Explanation |
|---:|---|---|
| 1 | `print("\nSklearn Accuracy:", accuracy_score(y_test, y_pred))` | Prints the calculated accuracy value. |
| 2 | `print("\nConfusion Matrix:\n", confusion_matrix(y_test, y_pred))` | Prints the confusion matrix showing correct and incorrect classifications. |
| 3 | `print("\nClassification Report:\n",` | Prints the given message or variable value for inspection. |
| 4 | `      classification_report(y_test, y_pred, target_names=iris.target_names))` | Stores the Iris class labels. |

